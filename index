<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Магазин косметики</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      margin: 0;
      padding: 0;
    }
    header {
      background: #ff4081;
      color: white;
      text-align: center;
      padding: 15px;
      font-size: 20px;
    }
    .container {
      padding: 10px;
    }
    .product {
      background: white;
      border-radius: 10px;
      padding: 10px;
      margin-bottom: 10px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .product img {
      max-width: 100%;
      border-radius: 8px;
    }
    .product-info {
      margin-top: 10px;
    }
    .product-title {
      font-size: 18px;
      font-weight: bold;
    }
    .price {
      color: #ff4081;
      font-size: 16px;
      margin: 5px 0;
    }
    .btn {
      display: inline-block;
      background: #ff4081;
      color: white;
      padding: 8px 12px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 5px;
    }
    #checkout {
      background: #4caf50;
      width: 100%;
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <header>🛍 Магазин косметики</header>
  <div class="container" id="product-list"></div>
  <button class="btn" id="checkout">✅ Отправить заказ</button>

  <script>
    const tg = window.Telegram.WebApp;
    tg.expand(); // развернуть WebApp на весь экран

    const products = [
      { name: "TIGI Balm 11", price: 2900, img: "https://via.placeholder.com/300x200?text=TIGI+11" },
      { name: "TIGI Balm 12", price: 3000, img: "https://via.placeholder.com/300x200?text=TIGI+12" },
      { name: "TIGI Balm 13", price: 3100, img: "https://via.placeholder.com/300x200?text=TIGI+13" },
      { name: "TIGI Balm 14", price: 3200, img: "https://via.placeholder.com/300x200?text=TIGI+14" },
      { name: "TIGI Balm 15", price: 3300, img: "https://via.placeholder.com/300x200?text=TIGI+15" }
    ];

    let cart = [];

    function renderProducts() {
      const list = document.getElementById('product-list');
      list.innerHTML = "";
      products.forEach(p => {
        const div = document.createElement('div');
        div.className = "product";
        div.innerHTML = `
          <img src="${p.img}" alt="${p.name}">
          <div class="product-info">
            <div class="product-title">${p.name}</div>
            <div class="price">${p.price} ₽</div>
            <button class="btn" onclick="addToCart('${p.name}', ${p.price})">Добавить</button>
          </div>
        `;
        list.appendChild(div);
      });
    }

    function addToCart(name, price) {
      cart.push({ name, price });
      tg.HapticFeedback.notificationOccurred("success");
      tg.showPopup({ title: "Корзина", message: ${name} добавлен в корзину!, buttons: [{ text: "Ок" }] });
    }

    document.getElementById('checkout').addEventListener('click', () => {
      if (cart.length === 0) {
        tg.showAlert("Корзина пуста!");
        return;
      }
      tg.sendData(JSON.stringify(cart));
      tg.close();
    });

    renderProducts();
  </script>
</body>
</html>
